# Улучшения компонента Header

## Обзор изменений

Компонент Header был значительно улучшен с точки зрения архитектуры, UX и доступности.

## Что было сделано

### 1. 📦 Вынесены константы
**Файл:** `src/utils/constants.ts`
- Добавлены константы языков `LANGUAGES` с полными названиями
- Теперь список языков хранится в одном месте и легко модифицируется

### 2. 🎨 Вынесены стили навигации
**Файл:** `src/utils/navigationStyles.ts`
- `navLinkStyles` - стили для десктопной навигации
- `mobileNavLinkStyles` - стили для мобильной навигации
- `bookButtonStyles` - стили для кнопки "Записатися" (десктоп)
- `mobileBookButtonStyles` - стили для кнопки "Записатися" (мобайл)

**Преимущества:**
- Нет дублирования кода
- Легко изменять стили в одном месте
- Соответствие принципу DRY

### 3. 🎯 Улучшен UX переключателя языков
**Файл:** `src/components/Header/LanguageSwitcher.tsx`

**Добавлено:**
- ✅ Закрытие выпадающего меню при клике вне его
- ✅ Закрытие по нажатию клавиши Escape
- ✅ ARIA-атрибуты для доступности:
  - `aria-expanded` - показывает состояние меню
  - `aria-haspopup` - указывает на наличие выпадающего меню
  - `aria-label` - описание для скринридеров
  - `role="menu"` и `role="menuitem"` для правильной семантики

### 4. ✨ Добавлена анимация мобильного меню
**Файл:** `src/components/Header/MobileMenu.tsx`
- Плавное появление/исчезновение меню
- Используется `transition-all duration-300 ease-in-out`
- Анимация `max-height` и `opacity`

### 5. ♿ Улучшена доступность (A11y)
**Файлы:** Все компоненты Header

**Добавлено:**
- `role="navigation"` для навигационных элементов
- `aria-label` для описания навигации
- `aria-expanded` для кнопок с выпадающими меню
- `aria-controls` связывает кнопку с меню
- `aria-hidden="true"` для декоративных SVG
- Динамические `aria-label` для кнопки меню ("Открыть/Закрыть меню")

### 6. 🔧 Созданы переиспользуемые хуки
**Новые хуки:**

#### `useClickOutside`
**Файл:** `src/hooks/useClickOutside.ts`
```typescript
useClickOutside(ref, handler, enabled)
```
- Обрабатывает клики вне элемента
- Работает с мышью и тач-событиями
- Можно включать/выключать динамически

#### `useEscapeKey`
**Файл:** `src/hooks/useEscapeKey.ts`
```typescript
useEscapeKey(handler, enabled)
```
- Обрабатывает нажатие клавиши Escape
- Можно включать/выключать динамически
- Удобен для закрытия модалов и выпадающих меню

## Структура компонентов

```
src/components/Header/
├── Header.tsx              # Главный компонент
├── Logo.tsx                # Логотип
├── DesktopNavigation.tsx   # Десктопное меню
├── MobileMenu.tsx          # Мобильное меню
├── MobileMenuButton.tsx    # Кнопка бургер-меню
└── LanguageSwitcher.tsx    # Переключатель языков

src/utils/
├── constants.ts            # Константы приложения
└── navigationStyles.ts     # Стили для навигации

src/hooks/
├── useClickOutside.ts      # Хук для клика вне элемента
└── useEscapeKey.ts         # Хук для клавиши Escape
```

## Преимущества новой архитектуры

### 🚀 Performance
- Использование `useCallback` для мемоизации функций
- Минимальное количество ре-рендеров

### 🔄 Переиспользование
- Хуки можно использовать в любых компонентах
- Стили навигации доступны везде
- Константы в одном месте

### 🎯 Поддерживаемость
- Легко найти и исправить баги
- Каждый компонент отвечает за одну задачу
- Понятная структура файлов

### ♿ Доступность
- Соответствие стандартам WCAG
- Поддержка скринридеров
- Навигация с клавиатуры

### 🧪 Тестируемость
- Маленькие компоненты легко тестировать
- Хуки можно тестировать отдельно
- Изолированная логика

## Как использовать

### Добавление нового языка
```typescript
// src/utils/constants.ts
export const LANGUAGES = [
  { code: 'ru', name: 'RU', fullName: 'Русский' },
  { code: 'uk', name: 'UK', fullName: 'Українська' },
  { code: 'en', name: 'EN', fullName: 'English' },
  { code: 'pl', name: 'PL', fullName: 'Polski' }, // Новый язык
] as const
```

### Использование хуков в других компонентах
```typescript
import { useClickOutside } from '@/hooks/useClickOutside'
import { useEscapeKey } from '@/hooks/useEscapeKey'

function MyModal({ isOpen, onClose }) {
  const modalRef = useRef(null)
  
  useClickOutside(modalRef, onClose, isOpen)
  useEscapeKey(onClose, isOpen)
  
  return <div ref={modalRef}>...</div>
}
```

## Дальнейшие улучшения (опционально)

1. **Анимации через Framer Motion** - более продвинутые анимации
2. **Фокус-ловушка** - удержание фокуса внутри мобильного меню
3. **Тесты** - unit-тесты для компонентов и хуков
4. **Storybook** - документирование компонентов
5. **Lazy Loading** - для улучшения производительности

## Совместимость

- ✅ React 18+
- ✅ TypeScript 5+
- ✅ React Router v6
- ✅ i18next
- ✅ Tailwind CSS

---

**Дата обновления:** 17 января 2026  
**Версия:** 2.0.0
