# React SPA App

Современное одностраничное веб-приложение на React с TypeScript, Vite и Tailwind CSS.

## 🚀 Технологии

- **React 18** - Библиотека для создания пользовательских интерфейсов
- **TypeScript** - Статическая типизация для JavaScript
- **Vite** - Быстрый инструмент сборки и dev-сервер
- **Tailwind CSS** - Utility-first CSS фреймворк
- **React Router** - Маршрутизация для React приложений
- **ESLint** - Линтер для JavaScript/TypeScript

## 📁 Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── Button.tsx      # Кнопка с вариантами стилей
│   ├── Card.tsx        # Карточка для контента
│   ├── Header.tsx      # Шапка сайта
│   ├── Footer.tsx      # Подвал сайта
│   └── Layout.tsx      # Основной макет
├── pages/              # Страницы приложения
│   └── HomePage.tsx    # Главная страница
├── hooks/              # Пользовательские хуки
│   └── useLocalStorage.ts
├── utils/              # Утилиты и хелперы
│   ├── constants.ts    # Константы приложения
│   └── helpers.ts      # Вспомогательные функции
├── types/              # TypeScript типы
│   └── index.ts        # Основные типы
├── assets/             # Статические ресурсы
├── App.tsx             # Главный компонент
├── App.css             # Стили приложения
├── main.tsx            # Точка входа
└── index.css           # Глобальные стили
```

## 🛠 Установка и запуск

### Предварительные требования

- Node.js (версия 16 или выше)
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

### Предварительный просмотр продакшен сборки

```bash
npm run preview
```

### Линтинг кода

```bash
npm run lint
```

## 🎨 Стилизация

Проект использует Tailwind CSS для стилизации. Основные классы:

- **Цвета**: primary-600, gray-50, white и др.
- **Размеры**: sm, md, lg, xl
- **Отступы**: p-4, m-2, space-x-4
- **Flexbox**: flex, items-center, justify-between
- **Grid**: grid, grid-cols-2, gap-4

## 🧩 Компоненты

### Button
Переиспользуемый компонент кнопки с вариантами стилей:

```tsx
<Button variant="primary" size="md" onClick={handleClick}>
  Нажми меня
</Button>
```

### Card
Компонент карточки для группировки контента:

```tsx
<Card title="Заголовок" className="bg-white">
  <p>Содержимое карточки</p>
</Card>
```

## 🔧 Настройка

### Добавление новых страниц

1. Создайте компонент страницы в `src/pages/`
2. Добавьте маршрут в `src/App.tsx`

```tsx
<Route path="/new-page" element={<NewPage />} />
```

### Добавление новых компонентов

Создайте файл в `src/components/` и экспортируйте компонент:

```tsx
export function NewComponent({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>
}
```

### Настройка Tailwind CSS

Измените `tailwind.config.js` для добавления кастомных цветов, шрифтов и других настроек.

## 📝 Скрипты

- `npm run dev` - Запуск dev-сервера
- `npm run build` - Сборка для продакшена
- `npm run preview` - Предварительный просмотр сборки
- `npm run lint` - Проверка кода линтером

## 🚀 Развертывание

### Vercel
```bash
npm install -g vercel
vercel
```

### Netlify
```bash
npm run build
# Загрузите папку dist на Netlify
```

### GitHub Pages
```bash
npm run build
# Настройте GitHub Actions для автоматического деплоя
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 🆘 Поддержка

Если у вас есть вопросы или проблемы, создайте Issue в репозитории.
